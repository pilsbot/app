
name: Dart CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    name: install dependencies
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: install dependencies
      uses: flutter/flutter@1.23.0-18.1.pre
      with:
        args: pub get

    - name: run tests
      uses: flutter/flutter@1.23.0-18.1.pre
      with:
        args: test

    - name: build apk
      uses: flutter/flutter@1.23.0-18.1.pre
      with:
        args: build apk --release
